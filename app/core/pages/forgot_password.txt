<?php

if ($page == 'forgot_password') {

   if (isset($_POST["submit"])) {
      $stmt = db_select('user',['*'],['email'=>$_POST['email']]);
      while ($row = $stmt->fetch()) {
         mail($_POST['email'], 'Reset Password', "<a href=\"$url&p=account&token=${row['password']}\">Reset Password</a>","Content-Type: text/html; charset=ISO-8859-1\r\n");
      }
   }

   $content .= <<<HTML

   <div>
<div class="error error_message">You must be logged in to access this page.</div><h2>Log in</h2>

<form action="#" name="login_form" method="post">
   <table class="login" cellpadding="4">
      <tbody>
      <tr>
         <td>Email:</td>
         <td>
            <input type="email" name="email" value="" class="text" style="cursor: auto;">
         </td>
      </tr>
      <tr><td colspan="2"><center><input type="submit" name="submit" value="Send Email" class="button"></center></td></tr>
      </tbody>
   </table>
</form>

   </div>

HTML;

}